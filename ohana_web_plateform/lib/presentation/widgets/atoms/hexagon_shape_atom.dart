import 'package:flutter/material.dart';

class hexagon_atom extends StatelessWidget {
  const hexagon_atom({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
        //decoration: BoxDecoration(shape: BorderRadius.all()),
        );
  }
}
