import 'package:flutter/material.dart';

class UnderlinedTitle extends StatelessWidget {
  const UnderlinedTitle({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
