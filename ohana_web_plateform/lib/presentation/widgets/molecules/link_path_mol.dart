import 'package:flutter/material.dart';

class LinkPath extends StatelessWidget {
  const LinkPath({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
